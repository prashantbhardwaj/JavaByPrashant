public class StaticMethodHolder {
    // Java doesn't require proper formatting
    public static int sum(int           val1, int val2) {
                            return val1 +              val2;
    }

    // access modifier - public
    // static or non static
    // return type - if nothing is getting returned then we will write void
    // method name
    // 0 or many arguments
    public static int substract(int val1, int val2) {
        return val1 - val2;
    }
}
